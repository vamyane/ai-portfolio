# 小红书评论工作台 · 更新日志

## v0.3.0（2026-09-03 · 整合版：0.2.11 搜索筛选 + 后续优化 + 双模式入口）
- ★ 核心：搜索/筛选功能完全恢复 0.2.11 基线——默认词库 = 0.2.11 原版逻辑（获客 AI 提示词生成搜索词 + 8 词兜底 + 62 匹配词逐字校准）
- ★ 筛选方式 = 2.11 原版（用户拍板）：**完全不打开、不点击小红书搜索页的筛选面板**（0.2.12 起 hover/click 面板那套已全部删除）——时间筛选靠代码级 maxNoteAgeDays 过滤（一天内=1 天 / 一周内=7 天 / 不限=9999），与 2.11 完全一致；位置（同城/附近）与 2.11 一致：不操作面板、不拼搜索词
- ★★ 整条找客户流水线 = 2.11 原版（用户拍板：从打开浏览器搜索到写评论，中间全部按 2.11）：core/engine.js 以 2.11 原版为底逐字恢复、core/ai.js 与 core/filters.js 直接换回 2.11 原版（哈希一致）——后续版本在这条线中间的改动全部删除：滚动 4 次懒加载（0.2.13）、低产止损（0.2.13）、按人设永久去重 processed-notes（0.2.12）、同行特征记忆注入（0.2.17）、webdriver 伪装（0.2.12）、定位授权（0.2.12）、hover 面板点击筛选（0.2.12）等；仅保留两处接线：词库双模块（kw-default=2.11 逻辑 / kw-custom 自定义页）与 SIGTERM 停止信号（停止按钮用）
- ★ 修复历史回归：剔除 0.2.13 起的 5 类筛选面板（排序/类型/范围），UI 筛选回到 0.2.11 的「时间 + 位置」两项；0.2.16 的"买家视角窄长尾词"、0.2.17 的 NEG_TITLE 误杀词、0.2.18 的"纯宽词"均不生效（默认词库封闭在 kw-default.js，只含 0.2.11 内容）
- ★ 新增：界面上方「🏠 默认找客户 / ✍️ 自定义搜索词」切换入口——默认页保持 0.2.11 功能不变；切到自定义页可用自己输入/生成的词搜索（我的词 + AI 拓展词 + 一键复用 + 主题保存复用 + 同样支持时间/位置筛选）
- ★ 新增（用户要求）：评论前「主页同行复核」——前面所有规则（匹配词/港险/地域/图片/AI 笔记评估）都通过之后、写评论之前，点进作者主页做最后一道同行判定：名字/简介强信号规则（保险规划师/经纪人/说保/聊保/服务全国/保单整理 等）→ 有 AI Key 时再做一次 AI 复核；命中即跳过不评论。今天实测拦截「XX爱说保」这类名字。实现为独立模块 core/profile-peer.js（不触碰 2.11 三件套），engine.js 仅两处接线（抓主页链接 + 判定调用）
- 保留后续优化（都在找客户流水线之外，不影响搜索行为）：进度内嵌开始按钮 + 停止按钮（0.2.15）、弹窗全部可关（0.2.13）、人设导入/导出与批量管理（0.2.12-0.2.13）、Word 防覆盖 + 保存目录记忆（0.2.12）、结果区「清空旧结果/看引擎日志」（0.2.12）、登录态实时校验、环境检测一键修复、主题切换、折叠模块化界面
- 说明：同行特征记忆库、低产止损、按人设永久去重已随流水线回退 2.11 一并移除（data/ 下的历史文件保留但不参与任务，无影响）
- 改动文件：core/engine.js（以 2.11 原版为底 + 词库模块接线 + 停止信号）、core/ai.js、core/filters.js（换回 2.11 原版）、core/kw-default.js（62 匹配词校准）、core/config.js（筛选 2 项 + 自定义词模式）、server/server.js（筛选保存 2 项）、web/index.html（顶部切换页签）、web/modules/se.js（筛选 UI + 自定义词区域）

## v0.2.17（2026-09-02）
- 新增：同行特征记忆库（自学习）——AI 每次识别到同行自动把案例（作者昵称/标题/识别理由）记入 data/peer-memory.json（保留最近 30 条），下次评估前自动注入 AI 提示词参考同类案例 → 越跑越准、越快识别同行
- 改进：标题负向词扩充（NEG_TITLE_RE 加：配置/保险方案/怎么配/科普/排行/产品对比/投保攻略/避坑指南/闭眼入等）——搜索页就滤掉更多同行科普帖，减少打开评估量、任务更快
- 说明：日志确认真实流程——搜索（买家词）→ 列表页预筛 → 打开评估 → AI 识别同行正确跳过（打开阶段看同行多是"先打开后判断"固有模式 + 搜索结果同行占比高）

## v0.2.16（2026-09-02）
- 修复：默认词库生成同行/科普词——用户反馈搜索词像"家庭保险配置/医疗险推荐"，搜出来大都是同行；根因：① themePrompt 含"咨询/购买/配置/选"销售词 ② AI 关键词提示词设 AI 为"保险获客运营专家"（销售视角）；修复：themePrompt 改纯买家场景、KW_PROMPT 重写（买家视角+正例清单+明确反例：严禁配置/测评/榜单/科普/推荐）、searchKwFallback 兜底扩充 12 个买家求助词；实测 AI 生成"新生儿保险怎么买/宝宝重疾险怎么选/小孩保险有必要买吗…"（买家口吻 ✓）

## v0.2.15（2026-09-02）
- 改进：任务进度内嵌「开始找客户」按钮——运行时按钮变 ⏳ 进度态（文字+进度条+找到 N 条/目标 M 条），不再有独立 banner 任务提示与按钮之间的视觉隔断（之前被搜索范围/登录两个折叠模块夹在中间）
- 新增：一键停止任务——旁边红底「■ 停止」按钮（运行态显示）→ `POST /api/cancel-collect` → server taskkill 进程树 + 引擎收到 SIGTERM 优雅退出 + pollProgress 改调 updateCollectProgress 更新按钮内进度

## v0.2.14（2026-09-02）
- 新增：代码模块化——5 个折叠模块拆为独立文件 `web/modules/{ai,pf,se,lg,rs}.js`（export const html 模板字符串），index.html 骨架留挂载点 `<div id="m-xx">`，主 JS `import` 各模块注入；改某模块只动对应文件，不影响其他模块和骨架
- 新增：server `/modules/` 子目录静态服务（原只支持 basename 文件，子目录 404 导致模块加载失败）
- 修复：搜索词不再 append「 同城/ 附近」——与筛选面板的「同城」重复导致「XX 同城」几乎搜不出；位置筛选完全靠筛选面板点击生效
- 修复：筛选下拉改动自动保存——用户改下拉（如一周内）忘点「保存筛选」时引擎仍用旧筛选；5 个筛选下拉 change 自动 POST /api/setfilter，立即生效
- 修复：完整冒烟测试发现自动保存代码在文件恢复时被覆盖丢失，已重新加回并验证（POST 自动保存正常）

## v0.2.13（2026-09-01）
- 修复：筛选排序没对上——UI 选「综合」但小红书保持默认「最新」；根因：applySearchFilter 把默认值 general 过滤掉不点，而小红书搜索默认是「最新」≠「综合」；改为排序类 force 必点（综合/最新/最多点赞…按用户选择真正点击），其他 4 类默认值=不限 不点
- 修复：筛选"同城"等选项未点中——hover 移动鼠标从「一天内」到「同城」时经过面板间隙会触发面板关闭；改为 force click 直接点击选项（鼠标保持在"筛选"按钮上面板不关）+ 点击后校验选中态（class=active）+ 面板意外关闭自动重新 hover 展开
- 修复：找客户找不到笔记/不翻页——引擎原来只抓搜索结果首屏卡片，首屏评估完就没了（同城+冷门词首屏更少）；现在打开搜索页后**模拟人手向下滚动 4 次触发懒加载**（跨 frame 滚到含卡片的那个 frame），收集更多卡片再评估
- 修复：筛选太严导致死循环——连续 3 轮无新候选 → 判定筛选条件过严、提前结束；有候选 → 按实际条数输出并提示"筛选较严格，仅找到 N 条"；一条都没有 → 明确报错"未找到符合条件笔记，请放宽筛选（时间/位置/搜索词）"
- 修复：所有弹窗必须有关闭按钮——xhsModal 加右上角「✕ 关闭」+ 点遮罩关闭；若弹窗是"同名冲突三选一"且用户直接关闭，自动视为取消
- 新增：新电脑/环境不达标首次打开自动强制环境检测——存在 fail 或 warn 自动弹出环境检测窗口，一键修复或关闭
- 新增：人设与署名「⚙ 管理」旁加常驻「📥 导入人设」——支持 .txt/.md/.json/.docx（Word），三级解析（内嵌JSON/Markdown段落/整篇兜底），同名三选一照常
- 新增：搜索筛选面板 5 类全接入（排序/类型/时间含半年内/范围/位置）——保存后引擎逐类自动勾选生效；时间代码级兜底支持半年内（180 天）
- 调整：搜索范围模块移到「开始找客户」上方（选完筛选直接找客户）；登录与通道移到开始找客户前
- 调整：折叠模块样式统一——圆角矩形+阴影+淡入动画（移除此前实验的梯形/内斜线/弹性动画）
## v0.2.12（2026-09-01）
- 🎉 修复：搜索筛选真正生效——**根因：筛选面板是 hover 悬停展开，不是点击**！之前的代码一直用 click 所以永远打不开面板；用户配合实测确认 + 监测器抓到面板结构后，重写 applySearchFilter：① hover `.filter` 展开面板（鼠标悬停展开、移开关闭）② 选项结构 `div.tags[data-hp-bound]` 精确匹配（避开 aria-hidden 不可见副本）③ 点选项前先 hover 到选项保持面板展开 ④ 同城/附近自动授权定位（grantPermissions + 北京坐标）——实测：「一天内(✓已选)」「同城」点击成功、筛选应用
- 新增：找客户时自定义搜索词 + 时间/位置筛选全部真正生效（时间 maxNoteAgeDays 兜底 + 位置搜索词 append 兜底仍在）
- 修复：今日客户笔记区「undefined · 默认风格 · 10 条」bug——`data.file` 字段不存在（file 在外层 r.file），改用 `_currentResultFile` 存储；resultInfo 增强为「时间 · 人设 · N 条 · 搜索来源」
- 新增：任务完成时检测是否生成了新文件（用文件 mtime 与启动时间比较），未生成则黄字提示「本次任务未找到结果，显示的是历史 N 秒前的旧结果」+ 给出日志路径建议
- 新增：结果区加「🗑 清空旧结果」按钮（POST /api/results-clear 删除 data/results/ 下所有今日整理-*.json，不删图片和引擎日志）和「📋 看引擎日志」按钮（提示日志路径）
- 新增：btnCollect 启动 banner 显示按「默认词库 / 自定义词 xx」搜索，让用户清楚跑的是什么
- 修复：搜索筛选 UI 点击改用 Playwright 真实点击（page.locator + click）——旧的 dispatchEvent 合成事件打不开新版小红书搜索页的"筛选"按钮；现在真实鼠标点击能触发 onClick 切换 URL 参数；同时给时间/位置选项点击失败加了日志（之前失败不打印看不出原因）
- 改进：位置筛选加搜索词兜底——选「同城/附近」时默认词库的搜索词自动 append「 同城/ 附近」（自定义搜索词不追加避免污染），配合时间 maxNoteAgeDays 代码级兜底，结果按筛选生效
- 改进：自定义词逻辑修正——勾选「自定义词」不再强制先填词（写词前不拦截）；选了但没填词时，点「开始找客户」才提示"未输入自定义关键词"，前端 + 服务端双拦截，引擎兜底报错退出（不静默跑默认）
- 新增：自定义搜索再升级——「我的搜索词」+「AI 拓展词」分开：自己写词点「💾 用我的词搜索」直接按它搜；词不够全点「🤖 AI 拓展搜索词」让 AI 生成更多词（填入下方独立拓展框，可删减/新增，<b>不覆盖你的词</b>），满意后「✅ 一键复用」生效；存 data/search-theme.json（userWords/aiWords/words 三组），config/engine 自动加载
- 新增：自定义搜索升级——输入框支持多个词同时输入（任意标点隔开：逗号/顿号/分号/空格等）+「🌐 默认词库 / ✍️ 自定义词」单选开关（默认词库）；切到自定义词并保存后，找客户只搜输入框里的词；「🤖 AI 生成」按主题生成一批词填入（存 data/search-theme.json，config/engine 自动加载）
- 新增：Word 保存目录记忆——上次输入的导出路径自动记住，刷新/重开软件自动回填（存 ui-settings.json）
- 新增：自定义搜索内容模块——输入主题（如"新生儿保险"）点「🤖 用 AI 生成搜索词」，AI 生成专属搜索词（search 8-12 个 + 匹配词），找客户时将只用这些词搜索；支持清除恢复默认；领导让搜什么就搜什么（存 data/search-theme.json，config/engine 自动加载优先使用）
- 新增：找客户「按人设永久去重」——同一人设（署名）处理过的笔记记录在 data/processed-notes.json，下次用同一人设跑任务自动跳过，避免重复骚扰同一客户；换人设则不受影响（人设A 去重不作用于 人设B）
- 修复：防御「无法第二次跑任务」——进程句柄残留但实际已死时（异常 kill），启动前探测并自动清理，不再一直提示"已有任务在运行"
- 改进：刷新成果按当次筛选为准——对比「当前筛选 vs 结果实际采用的筛选」，不一致时明确提示（"当前结果对应一周内，与当前筛选不一致，请重新找客户"）；刷新按钮金色高亮更醒目
- 改进：Word 文档命名防覆盖——同一天多批次/同一次重复导出自动加 -2/-3 后缀，绝不覆盖旧文档
- 新增：人设 key 改为唯一 id（支持同名多个人设）；蒸馏保存时检测同名 → 三选一弹窗：🔗合并（更新被蒸馏字段、保留原署名/私信）/ ♻️覆盖 / ➕新建同名
- 新增：人设管理模式升级——✎ 改名（非默认人设）+ 勾选批量删除 + 全选；默认人设仍不可删/改
- 新增：文档蒸馏一次出两种——选一份文档同时提取「评论风格 + 私信风格」，分别填入两个提示词框
- 新增：蒸馏状态标注——文档蒸馏按钮完成后变为「重新蒸馏」并标「✅ 已蒸馏」；小红书蒸馏按钮下方显示已蒸馏账号列表（名字/档位/时间）
- 新增：人设导出/导入——管理模式勾选后「📤 导出所选」生成 .md 文档（人可读+内嵌 JSON），换电脑「📥 导入人设」即可恢复，不用重新蒸馏
- 兼容：旧版 profile-config.json（key=人名）自动补 id，无需迁移
- 说明：改动涉及 core/config.js、core/engine.js、server/server.js、web/index.html——server 有改动需重启工作台；前端刷新即生效

## v0.2.11（2026-08-31）
- 新增：新电脑零配置兜底——打包会排除 data\，全新安装后没有 user-config.json，此前装完会显示「AI 未接入」需手动点一次免费通道；现 server 首次启动时自动写入默认免费 AI 配置（智谱 glm-4-flash + 内置 key），装完开箱即用
- 说明：仅影响全新安装；本机/老用户已有配置完全不动（文件存在即跳过，不影响现有 DeepSeek/自定义配置）；已实测模拟新电脑场景：启动后自动生成配置、/api/ai-status 返回 configured=true / provider=zhipu / model=glm-4-flash
- 改动文件：仅 server/server.js（启动初始化 ensureDefaultAIConfig）

## v0.2.10（2026-08-31）
- 修复：读取进度"已读 0 篇"不增长——根因：原实现只有「成功抓到正文」才计数（notes.push + 写进度），点开了但正文没抓到的一律不计，导致浏览器一直在翻、界面一直 0
- 改进：计数规则改为「以浏览的笔记数为准」——点开一条详情就 +1（clicked=true 即计数，与正文抓取成功与否无关），进度弹窗实时增长；成功抓到正文的另计入素材数（供蒸馏用）
- 改进：档位达标判定（轻量 8/普通 20/超级读完）与超时、防死循环判定全部改用「浏览数」——浏览数到档位目标即停，不再被"抓空不计数"卡住
- 改进：界面文案区分两个数——进度显示「已浏览 X 篇」，完成汇报显示「已浏览 N 篇（成功抓到正文 M 篇）」，蒸馏弹窗用素材数 M 说明分析规模
- 说明：改动涉及 core/xhsDistill.js、server/server.js、web/index.html——server 有改动需重启工作台，前端刷新即生效

## v0.2.9（2026-08-31）
- 修复：读取"已读 0 篇、永远停不下来"的死循环——根因：每篇打开后若抓不到正文（r.ok=false），已读数永远是 0、目标永远达不到；而每轮都点过卡片导致"空转计数"一直被重置，普通/轻量模式又无超时，只要主页滚动一直有新卡片就无限循环
- 修复：点击卡片后改为「等待详情容器真正出现」（waitForSelector，8 秒），等不到立刻诊断失败，不再盲等固定时长后抓空
- 新增：防死循环兜底——连续失败 6 篇、或成功 0 篇但已试 12 篇，自动停止并提示「读取逻辑可能与页面结构不匹配，请看诊断日志」
- 新增：抓空/详情未出现时输出诊断日志（URL、是否命中详情元素、页面开头文字），方便针对性修选择器
- 改进：轻量/普通档位也加总超时（轻量 10 分钟、普通 15 分钟）作为双保险，任何情况下都不会无限跑
- 新增：读取进度弹窗内加「✕ 取消读取」按钮——原取消按钮在弹窗下方的主界面里，模态弹窗打开时被遮住点不到；现在弹窗内可直接取消（事件委托绑定，热生效刷新即用）
- 说明：改动仅 core/xhsDistill.js 与 web/index.html，热生效，重新点「开始读取」即生效；若仍抓空，把诊断日志发给开发者

## v0.2.8（2026-08-31）
- 新增：蒸馏深度三档按钮（轻量/普通/超级）——不再用 AI 判定读多少篇，改由档位决定：
  - 🌱 轻量蒸馏：目标 5-10 篇（目标 8、封顶 10），最快出风格，适合只要个大概
  - ⚡ 普通蒸馏：目标约 20 篇（封顶 20），速度与风格完整度均衡（默认推荐）
  - 🚀 超级蒸馏：读完该账号全部已发布笔记+评论区回复，最多 30 分钟；连续 3 轮滚动无新卡片即判定读完、自动提前结束（不会傻等满 30 分钟）
- 新增：智能数量汇报——读取完成提示实际篇数：若选超级/普通但该账号实际只有几篇，明确提示「该账号共 N 篇，已全部读完」；不足目标篇数时提示「实际仅 N 篇，已尽力读完所有可见笔记」，让用户清楚蒸馏数量级
- 新增：读取进度实时显示——读取过程中每 2.5 秒刷新「已读 X 篇 / 目标 Y 篇」（超级模式显示已读 X 篇·用时 N 分 N 秒·上限 30 分钟）
- 新增：读取中途可真正取消——「取消」按钮改为 taskkill 终止整个读取进程树（原取消只删记录、进程还在后台跑），并恢复界面状态
- 新增：同号蒸馏去重——同一小红书号再次蒸馏保存前，检测到已存在该账号的蒸馏人设，询问是否覆盖，避免误生成重复人设
- 改进：详情页抓取力度按档位——轻量每篇滚 3 屏/博主回复 5 条，普通 5 屏/8 条，超级 8 屏/20 条
- 改进：生成的人设记录蒸馏档位（distillMode），人设标题提示来源档位
- 新增：进主页先探测笔记总数——打开账号主页时读取「笔记 N」计数（正文/标签/页面文字多级匹配），开跑前进度弹窗即显示「主页显示约 N 篇」，读取完成汇报同样带上数量级
- 新增：超级蒸馏风控节奏放慢——关键等待间隔 ×2（滚动、点击、返回、翻页全部放慢，连续长时间读取更像真人，降低被风控概率）；轻量/普通保持原节奏
- 新增：人设按钮档位角标——蒸馏生成的人设按钮右侧加小角标「🌱轻量/⚡普通/🚀超级」，不改按钮名字；旧人设无 distillMode 则不显示
- 修复：人设栏「⚙ 管理」按钮位置漂移——原首次渲染在右侧、点管理后被人设按钮重建挤到左侧；现每次渲染固定钉在「人设：」标签后（最左侧）
- 修复（v0.2.8b）：详情滚动滚错对象——小红书笔记详情是居中弹层（fixed 定位），原用 window.scrollBy 滚的是背景页面（笔记列表），弹层正文/评论区懒加载永远不触发，只能抓到首屏；现改为定位弹层内部包含正文/评论区的可滚动容器并滚动其内部，找不到时兜底找可滚动面积最大的元素，滚不动即到底
- 说明：server.js 有改动，需重启工作台后生效；core/xhsDistill.js 与 web/index.html 改动刷新即生效

## v0.2.7（2026-08-31）
- 修复：蒸馏小红书笔记风格时详情页全部 404——原实现是「扫 DOM 收集笔记 ID → 直接 goto 详情 URL」，无 referer、无前置点击，极易被小红书 404/登录墙拦截（笔记真实存在也被伪装成 404）
- 改进：读取方式改为「模拟人手点击」——进主页后逐篇点击笔记卡片（真实鼠标事件走 SPA 路由进详情），抓完标题正文按 Esc/返回主页再点下一篇，与真人操作一致，绕过 404 墙
- 新增：404/登录墙/风控墙识别——详情页出现「404/登录后查看/请扫码用手机」等特征时明确报错并中止，不再闷头刷 12 篇
- 新增：启动参数加 --disable-blink-features=AutomationControlled 去除自动化指纹，降低被风控概率
- 修复（v0.2.7a）：点击式读取卡住不动——① goBack 不能等 domcontentloaded（SPA 历史回退不重新加载文档，空等 30s 超时），改短超时 + 固定等待；② 卡片收集双选择器兜底：a[href*=/explore/] 链接 + section.note-item[data-note-id] 卡片（与找客户同款）；③ 点击加 noWaitAfter、失败 force 兜底，兼容详情在新标签页打开；④ 每步打日志，卡在哪一眼可见
- 修复（v0.2.7c）：蒸馏读取只抓首屏——进入详情页后模拟人手向下滚动 4 屏加载全文+评论区再抓取，正文取满 4000 字；并新增抓取「博主评论区回复」（作者自己的评论=最地道的口语风格，最多 5 条并入蒸馏素材）
- 新增：人设管理模式——人设栏新增「⚙ 管理」按钮，点击后人设按钮跳动、右上角出现 ×，点 × 删除该人设（默认人设不可删；删除当前激活人设自动回退默认；删完自动退出管理模式）
- 改进：AI 评论字数硬性上限 250 字（小红书上限 300 字留余量）+ 写完自动快速校验：超 250 字自动在标点处截断到 250 字内，不足 10 字自动扩写重写一次；engine 日志同步打印超长警告
- 改进：评论紧扣主题（硬性）——SYSTEM_PROMPT 新增第 11 条：评论必须与笔记主题直接相关，禁止万金油话术/答非所问，笔记讲什么就答什么
- 说明：改动涉及 core/xhsDistill.js、core/ai.js、core/engine.js、web/index.html，均热生效（server 无需重启）；若仍被墙，请先重新扫码登录

##v0.2.6（2026-08-31）
- 修复：搜索筛选（"一天内/一周内/同城/附近"）从未生效——clickText 用精确文本匹配+原生 .click()，但小红书网页端"筛选"按钮 textContent 含图标不纯等于"筛选"，且 React 组件不响应原生 click
- 改进：clickText 改宽松匹配（精确/包含/startsWith 多级兜底）+ 触发方式改为 mousedown→mouseup→click 三连 dispatchEvent，React 组件响应
- 新增：searchFilter.time 映射到 maxNoteAgeDays 代码级兜底——即便页面点击失败，选「一天内」就真只收 1 天内帖子（之前固定 7 天）
- 备注：探针实测被小红书风控（"安全限制IP存在风险"）拦截，无法 headless 验证；用户工作台已登录，升级后跑一次看日志"搜索筛选：时间=「一天内」"出现即生效

## v0.2.4（2026-08-31）
- 修复：小红书实际已退出登录/登录过期时，主界面仍显示「已登录」——页面加载时若显示已登录会实时校验一次，失效立即纠正
- 修复：点「开始找客户」前强制校验真实登录态——未登录直接拦截提示，不再白白跑 20-40 分钟
- 优化：登录校验结果即使状态文件被占用也能正常返回

## v0.2.3（2026-08-31）
- 修复：覆盖安装时提示「无法打开要写入的文件 node.exe」——安装器现在会在写文件前自动关闭运行中的旧版本（taskkill /T 杀启动器进程树 + 残留 node），等待 5 秒释放文件锁后再拷贝
- 修复：系统托盘图标一直是默认小红点——Tauri launcher 从 `/disticons/128x128.png` 加载托盘图标，但 server 没有这个路由；现已添加该路由返回新应用图标（对话气泡设计）

## v0.2.2（2026-08-31）
- 修复：应用图标未在网页端生效——浏览器标签页 favicon 之前返回空内容（204），现已返回真实图标；网页顶部 header 的「保」字 logo 替换为新应用图标
- 修复：任务栏/桌面 exe 图标缓存刷新，确保新图标（对话气泡设计）在所有位置一致显示
- 优化：server 新增 /favicon.png 路由（供网页 header logo 使用），MIME 类型补充 .ico/.svg

## v0.2.1（2026-08-31）
- 重做：应用图标全新设计——红色渐变圆角方块 + 超大白色对话气泡（占65%面积）+ 气泡内三条评论横线 + 右上角未读红点；任务栏小尺寸（16/24/32）下清晰可辨，不再缩成"一个红点"；ICO 含 10 个尺寸，放大不糊
- 新增：历史评论/聊天风格蒸馏进度条后增加「取消」按钮，选错文件或不想等了可随时中止
- 修复：AI 连接检测 toast 在亮色/米白主题下深色字压深色底看不见的问题
- 修复：「蒸馏我的历史评论/聊天风格」按钮与「启用蒸馏风格」开关重叠——删除两个启用开关，蒸馏按钮本身用发光边框表示已启用状态
- 优化：小红书笔记蒸馏完成后，命名弹窗明确说明「该名字同时用作人设按钮名和私信自称（署名）」；人设新增 source 字段区分「手动」与「小红书蒸馏」来源

## v0.2.0（2026-08-31）
- 新增：界面改版——所有模块变为可折叠/展开的条（除「每人找多少条」和四个主按钮外），页面不再需要一直往下滑
- 新增：「主题切换」——顶部一键切换 默认 / 纯黑 / 经典亮色 / 米白 四种配色，切换时字体颜色自动适配，防止看不清
- 新增：主题色与折叠/展开状态自动记忆，下次打开保持上次设置
- 新增：人设与署名重构——不再固定同事A/B，改为「默认」+ 可无限新增的自定义人设（按私信署名命名按钮）；点按钮切换、保存即新增/更新
- 新增：结果不再分两份文档——一次任务一份 Word，按「默认风格 / 某署名风格」命名；每条笔记仍保留 2 种风格评论（专业顾问型 / 亲切贴心型）
- 新增：「蒸馏小红书笔记风格」——输入小红书号，AI 读取该账号历史笔记并模仿其说话风格，自动生成新的人设按钮（风格明显少读约 6 篇、不明显多读最多 12 篇）
- 新增：登录区增加醒目红色警示「请勿使用小红书主号登录，避免惩罚」
- 修复：「蒸馏历史评论」按钮与「启用蒸馏风格」开关重叠、按钮无形状的问题
- 修复：同行识别漏判——AI 评估连续 3 次失败时直接中断任务并提示用户（不再"宁可放过"放行）
- 优化：AI 接入说明更新——内置免费版可直接用；DeepSeek 需自填 API Key（效果更好）
- 优化：AI 引擎默认通道切回 DeepSeek、结果 JSON 解析加固、弱模型坏输出检测
- 修复：AI 连接检测 toast 在亮色/米白主题下深色字压深色底看不见的问题（新增 --toast-text 变量，四套主题均适配）
- 修复：「蒸馏我的历史评论/聊天风格」按钮与「启用蒸馏风格」开关重叠——删除两个启用开关，蒸馏按钮本身用发光边框表示已启用状态
- 优化：小红书笔记蒸馏完成后，命名弹窗明确说明「该名字同时用作人设按钮名和私信自称（署名）」；人设新增 source 字段区分「手动」与「小红书蒸馏」来源，蒸馏人设按钮前带 📱 标记
- 新增：历史评论/聊天风格蒸馏进度条后增加「取消」按钮，选错文件或不想等了可随时中止（服务端检测到客户端断开会自动停止分析）

## v0.1.7（2026-08-25）
- 新增「📜 更新日志」入口：右上角按钮随时查看每个版本更新了什么

## v0.1.6（2026-08-25）
- 修复：扫码登录成功后，登录卡片仍显示"尚未启动登录"的问题（改为"已登录"样式）
- 新增：「🔁 一键切换账号」——清空登录态重新弹二维码扫码，或手动在登录浏览器里退出登录换号

## v0.1.5（2026-08-25）
- 新增：「🤖 AI 连接检测」按钮——免费通道实测连通性、付费通道（DeepSeek）没填 Key 自动提示去填写

## v0.1.4（2026-08-25）
- 新增：三个免费 AI 通道（智谱 / 硅基流动 / 阿里通义）内置 Key，无需填写直接可用
- 优化：AI 接入面板对已内置通道隐藏 Key 输入框，小白更简单

## v0.1.3（2026-08-25）
- 修复：升级安装时提示"无法打开要写入的文件"的问题（安装器会自动关闭运行中的旧版本）
- 优化：欢迎页提示安装过程会自动关闭工作台

## v0.1.2（2026-08-25）
- 新增：右上角「🖥 环境检测」——一键体检 Node / Python / 浏览器 / AI / 数据目录五项环境
- 新增：环境不达标可一键修复（自动安装 Python、python-docx、浏览器内核）
- 修复：生成 Word 时"找不到 Python 环境"的问题（改为自动探测 Python，不再写死路径）

## v0.1.1（2026-08-25）
- 新增：历史评论/聊天「风格蒸馏」——上传你的历史评论（txt / word / csv），AI 自动提炼个人写作风格
- 新增：评论/私信人设一键填入蒸馏结果 + 启用开关，下次任务按你的风格写
- 新增：蒸馏过程实时进度条，上万条评论分批处理不卡顿

## v0.1.0（2026-08-23）
- 首个版本：自动找正在咨询保险的客户笔记，写好评论和私信，照着发就行
- 双风格评论（专业顾问型 / 亲切贴心型）+ 高潜客户私信
- 扫码登录一次长期有效；A/B 分组互不重复；一键导出 Word 任务清单
